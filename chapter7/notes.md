annotations -> top level types 
for the exam: 
@Deprecated
@SuppressWarnings
@SafeVarargs

interface - abstract data type that declares a list of abstract methods that any class implementing the interface must provide

interface keyword used for declaring one 
abstract - implicit modifier 
public abstract - also implicit modifiers (mehtod)
public static final - implicit modifiers (constants) - can be used outside the interface declaration without requiring an instance of the interface 

interface variables are referred to as constants because they are assumed to be public, static, and final 

overridden interface method -> covariant return type
covariant return type -> same or subtype 
